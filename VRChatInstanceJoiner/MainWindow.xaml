<Window x:Class="VRChatInstanceJoiner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VRChatInstanceJoiner"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="VRChat Instance Joiner" 
        Height="600" 
        Width="900"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">
    
    <materialDesign:DialogHost Identifier="RootDialog">
        <DockPanel>
            <!-- Top Menu Bar -->
            <materialDesign:ColorZone Mode="PrimaryMid" 
                                    Padding="16" 
                                    DockPanel.Dock="Top">
                <DockPanel>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="VirtualReality" 
                                              Height="32" 
                                              Width="32" 
                                              VerticalAlignment="Center" />
                        <TextBlock Text="VRChat Instance Joiner" 
                                VerticalAlignment="Center" 
                                Margin="16,0,0,0" 
                                FontSize="20" />
                    </StackPanel>
                </DockPanel>
            </materialDesign:ColorZone>
            
            <!-- Main Content -->
            <Grid Margin="16">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="VRChat Instance Joiner" 
                             FontSize="32" 
                             FontWeight="Bold" 
                             HorizontalAlignment="Center"/>
                    
                    <TextBlock Text="Application is running successfully!" 
                             FontSize="18" 
                             Margin="0,16,0,32" 
                             HorizontalAlignment="Center"/>
                    
                    <materialDesign:Card Padding="32" Margin="0,16,0,0">
                        <StackPanel>
                            <TextBlock Text="Status" 
                                     FontSize="18" 
                                     FontWeight="Bold" 
                                     Margin="0,0,0,16"/>
                            
                            <TextBlock Text="• Dependency Injection: Working" Margin="0,4"/>
                            <TextBlock Text="• Material Design: Working" Margin="0,4"/>
                            <TextBlock Text="• Services: Initialized" Margin="0,4"/>
                            <TextBlock Text="• UI: Rendered" Margin="0,4"/>
                        </StackPanel>
                    </materialDesign:Card>
                </StackPanel>
            </Grid>
        </DockPanel>
    </materialDesign:DialogHost>
</Window>
