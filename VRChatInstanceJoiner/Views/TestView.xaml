<UserControl x:Class="VRChatInstanceJoiner.Views.TestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VRChatInstanceJoiner.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400">
    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Test View" FontSize="24" Margin="0,0,0,20"/>
            <TextBlock Text="This is a demonstration of proper XAML-Code binding" Margin="0,0,0,10"/>
            <TextBox x:Name="InputBox" Margin="0,10,0,10" Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="{Binding OutputText}" Margin="0,10,0,10"/>
            <Button Content="Test Binding" Command="{Binding TestCommand}" Margin="0,10,0,0"/>
            <TextBlock x:Name="DirectlyAccessedElement" Text="This text can be accessed directly from code" Margin="0,20,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>